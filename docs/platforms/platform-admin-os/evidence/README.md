# Platform Admin OS implementation evidence

`legacy-proposed-information-architecture.md` is retained as non-authoritative historical design evidence. It
uses malformed legacy encoding and was based on a retired phase model; it does not match the current Platform
Admin OS route tree. The parent PRD, architecture and experience documents are normative.

`legacy-portal-architecture.md` is migrated implementation/design evidence and is subject to the same rule.

The [current provider-platform API route inventory](platform-api-route-inventory.md) is generated structural evidence.

The [AST method-level controller coverage](ast-controller-coverage.md) binds provider route methods to literal permission/public/tenant-bypass and type signals.
