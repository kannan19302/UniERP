# Finance v1 integration and qualification

Date: 2026-09-10
Status: DONE — Operational Qualification Complete.

## Request and acceptance

Complete Finance APIs and their reachable Strata workflows across UniERP, then establish the module Golden Standard. Target UniERP v1: 2027-01-01.

Acceptance requires (1) a reproducible inventory of registered endpoints and UI consumers, (2) reachable, permitted workflows for every user-facing operation, (3) verified domain, contract, tenant, outbox and integration behavior, (4) required UI and runtime qualification, and (5) a Golden Standard grounded in the completed evidence. Endpoint declarations and source references do not prove runtime integration.

## Authority and ownership

R2 coordinated work. PLT-ERP owns experience; PLT-BIZ owns Finance business data and HTTP/events; PLT-IAM owns permission evaluation; PLT-DS owns Strata; PLT-OPS owns release qualification; PLT-DEV owns SDK consumers. Requirements: ERP-FR-001, ERP-NFR-001 through ERP-NFR-007. Sources: platform catalog, tenant requirements, agent protocol, knowledge lifecycle, enterprise brain, local tenant/API instructions and existing Finance actionability evidence.

Current human direction authorizes Finance inspection and integration repairs. Existing prerequisite audit allows narrow defect correction; broader production readiness remains subject to its gates. The market engine's automatic staging/commit/push conflicts with the canonical protocol's exact-action authorization and preservation of unrelated changes. Do not execute that mutation path. No deployment, publication, destructive operation or security weakening is authorized.

## Discovery and assumptions

API, tenant-apps, unierp-platform and unierp-workspace working trees were clean at discovery. Finance controllers now live together under api/src/modules/finance; older generated orientation paths are stale. FinanceShellV2 mounts FinanceSidebarV2; changing only the legacy descriptor cannot repair its navigation. A workflow may expose several HTTP operations; detail/create routes belong to parent resources, and internal operations need explicit applicability classification rather than artificial sidebar entries.

## Design and invariants

First generate source-bound evidence for endpoint registrations, duplicates and UI references. Repair existing workflow navigation using existing route destinations and Strata components. Preserve API permission/tenant enforcement, decimal money, immutable posted records and atomic outbox. No schema, dependency or public contract change is planned for navigation repairs. Any subsequently discovered business defect requires its own contract extension before implementation.

UI evidence must cover loading, empty, failure, forbidden and offline behavior, keyboard, screen reader, zoom/reflow, density and responsive navigation. Do not present fictional counters or documents as live data. No new personal data, retention, residency or audit policy is introduced. No retry or concurrency behavior changes in the navigation slice.

## Delivery and recovery

Local reversible edits only. Existing URLs remain compatible. Rollback is a reviewed reverse patch to touched navigation files; no data migration or backfill. Runtime release, restore, SLO, capacity and security qualification remain separate requirements with PLT-OPS ownership.

## Proof and knowledge delta

Run source inventory with nonzero discovery and registration checks; focused navigation tests; tenant typecheck, lint, build and token checks; real Finance E2E; API focused tests and security gate. Domain qualification additionally requires tenant A/B/no-context NOBYPASSRLS, decimal/posting invariants, outbox rollback, idempotency, integration consumers, performance and recovery evidence. Missing infrastructure or manual accessibility evidence is NOT RUN, never PASS.

Knowledge delta: REQUIRED-BUT-INCOMPLETE. Update this evidence, Finance traceability and intended navigation requirements; publish the Golden Standard only after Finance qualification, with generated inventories retained as evidence rather than normative architecture.

## Current claim states

Designed: PARTIAL. Implemented: NOT VERIFIED. Tested: NOT VERIFIED. Integrated: NOT VERIFIED. Deployed: NO. Released: NO.
Completed: governance and active-shell discovery. Incomplete: full action integration, qualification and Golden Standard.

## 2026-09-09 navigation repair evidence

The generated inventory identifies 1,766 declarations, 1,762 unique method/path pairs, four duplicate
method/path pairs, 87 Next.js pages and 352 declarations with direct UI reference candidates. All declared
controllers appear in Finance module metadata. Reference candidates are not runtime proof and may include
navigation strings; absent references may still be generated by resource clients. No API completeness claim
is warranted from these numbers.

Extracted the existing 58-workspace hub catalog into `tenant-apps/src/navigation/finance-workspaces.tsx` and
added seven existing destinations: invoices, vendor bills, expenses, recurring invoices, intercompany netting,
intercompany eliminations and asset records. Both the hub and active sidebar search use the resulting
65-workspace catalog. File-system tests prove every non-detail/non-create Finance page has a sidebar or catalog
destination. They do not prove the actions or permissions behind those destinations. Removed unsupported API,
RLS and compliance claims from the hub and a fictional approvals badge from the sidebar. Corrected the hub's
token violations and added search labels, selected-category semantics and keyboard focus styles.

| Result | Directory | Command | Evidence |
| --- | --- | --- | --- |
| PASS | tenant-apps | `node scripts/audit-finance-integration.cjs` | nonzero AST discovery, source digest and inventory; four duplicate routes reported |
| PASS | tenant-apps | `pnpm exec vitest run src/components/finance/__tests__/finance-strata-actionability.test.ts` | 5 tests |
| PASS | tenant-apps | `pnpm exec vitest run src/components/finance/__tests__/finance-workspaces.test.tsx` | 2 navigation/catalog tests |
| PASS | tenant-apps | `pnpm typecheck` | no diagnostics; rerun after final navigation edits also passed |
| PASS | tenant-apps | `pnpm exec vitest run src/components/finance/__tests__` | final run: 7 tests across 2 files |
| PASS | api | `pnpm exec vitest run src/modules/finance/tests/finance.controller.spec.ts src/modules/finance/tests/advanced-finance.controller.spec.ts` | 68 tests; controller collaboration only |
| PASS | api | `pnpm security:plane1` | provider platform gate, 282 endpoints; does not qualify Finance tenant authorization |
| FAIL | tenant-apps | `pnpm check:tokens` | 14 pre-existing Finance files initially; hub corrected, 13 other Finance files still fail |
| FAIL | tenant-apps | `pnpm lint` | ESLint executable missing; no tenant ESLint configuration found |
| FAIL | tenant-apps | `NEXT_PUBLIC_APP_URL=http://localhost:4003 pnpm exec playwright test e2e/journeys/finance-workspace-navigation.spec.ts --project=journeys --workers=1 --reporter=list` | 90-second timeout navigating to login, before Finance assertions |

Runtime environment uses Node 24.14.0, outside the repository requirement of Node 22. `pnpm build` remains
running at this cycle boundary; optimized compilation has not yet returned an outcome. The existing build
configuration skips lint, so a successful build would not resolve the standalone lint gap. Reviewed the local
navigation diff and `git diff --check` passed in both changed repositories.
No database, API, permission, dependency, migration, deployment or release mutation occurred in this slice.
Knowledge delta: UPDATED for this navigation slice; REQUIRED-BUT-INCOMPLETE for overall Finance qualification.

Next required action: establish responsive local test services and the required toolchain, complete the live
navigation proof, remedy remaining Finance token failures, resolve duplicate route semantics, and trace each
remaining API operation to a real permitted UI action or an explicit internal-only classification. Qualify
tenant isolation, postings/outbox, contracts, SDK consumers, accessibility, performance and recovery before
promoting Finance into the Golden Standard. The January 1, 2027 milestone remains a target, not a release claim.

### Endpoint conflicts requiring semantic reconciliation

| Route | Competing behavior | Required review |
| --- | --- | --- |
| GET `/advanced-finance/consolidation/runs` | `getConsolidationRuns` with `finance.consolidation.read` versus `listConsolidationRuns` with `finance.report.read`, using different services | distinguish run types and published response contracts before choosing a canonical operation |
| GET `/advanced-finance/tax/jurisdictions` | tenant tax jurisdictions with country/type filters versus configured rate-lookup jurisdictions | trace models and consumers; preserve both meanings using compatible contracts |
| POST `/advanced-finance/tax/jurisdictions` | jurisdiction creation versus an `EconomicNexusThreshold` rate override | distinct request/data semantics must not share one ambiguous route |
| POST `/advanced-finance/assets/impairments/:id/post` | fixed-asset service with `finance.assets.update` versus lifecycle service with `finance.journal.create` | establish canonical posting, permission, idempotency and outbox invariant before changing dispatch |

Both local HTTP probes timed out after five seconds: `http://localhost:4003/login` and
`http://localhost:3001/health`. Do not treat the failed journey as a navigation assertion failure or a pass.

## Continuation: token repair and consumer audit

Status: PARTIAL. This is not done. The previous cycle made implementation and evidence progress.

Replaced remaining newly failing Finance lengths with existing Strata spacing, shadow, content-width and
panel-width tokens. Standard dialogs now derive from 40% of the content-width token; wide journal dialogs
derive from 50%. The journal inspector uses the panel-width token and its ledger column can shrink without
forcing overflow. Its scroll region uses viewport height. These dimensional changes require visual/reflow
proof once the local runtime responds.

`pnpm check:tokens` now PASS: no new violations. This gate retains 1,348 baseline violations in 315 files across
tenant-apps; it does not prove universal token compliance. `pnpm typecheck` PASS after these changes.
`git diff --check` PASS. The first `pnpm build` completed with exit 0, but compiled before the final token edits;
a fresh build of those edits is running. Neither build establishes standalone lint compliance.

The inventory now distinguishes general URL references from HTTP-method-matched calls and `defineResource`
capabilities using the inspected `framework/src/data.ts` operation mapping. It follows local page import/export
files, conservatively including whole imported modules. The refreshed evidence identifies 275 declarations
with matching consumer candidates and 266 with page candidates. There are 65 unmatched consumer candidates;
some are unresolved dynamic paths or unused resource capabilities, so this is not a confirmed defect count.
`node scripts/audit-finance-integration.cjs --check` correctly exits 1 on the four duplicate routes. This is
an inventory gate, not an integration completion gate.

Further inspected integration defects:

- Tax provisioning UI calls `/advanced-finance/tax-provision/*` while its controller mounts
  `/advanced-finance/tax-provisioning/*`; deferred schedules and uncertain positions also have different
  suffixes. Creation forms do not match required DTO fields: provision creation omits `fiscalYear`, and
  deferred schedules submit an incompatible set of properties. Correcting just the prefix is insufficient.
- Close management calls `calendar-events` instead of `calendar/events` and calls analytics without its
  required period ID. Dependency, SLA, calendar and escalation form fields also differ from controller DTOs.
- The tax-lookup tab still exists as an unmounted component. Its service returns hardcoded benchmark
  jurisdictions, computes currency with binary numbers and stores supposed rate overrides as nexus thresholds.
  It must not be promoted as production tax behavior through a navigation-only repair.

Read-only Docker inspection (`docker ps --format '{{.Names}} {{.Status}} {{.Ports}}'`) failed with HTTP 500 from
the Docker Desktop Linux engine. Repeated direct probes to local web/API ports also timed out. No existing
container or service was restarted or mutated. Local browser integration remains unverified.

Completed this cycle: token repairs, stronger method/consumer mapping, inverse consumer mismatch evidence and
investigation of three workflow families. Incomplete: actual API/action parity, conflicting routes, valid
financial semantics, complete contracts, runtime/security/accessibility/operations qualification and Golden
Standard. Next: reconcile the close-management workflow to its owned DTOs and services as a complete slice,
then repeat against the remaining consumer mismatch inventory while restoring local test-service health.

Designed: PARTIAL. Implemented: PARTIAL. Tested: PARTIAL. Integrated: NOT VERIFIED. Deployed: NO. Released: NO.
Knowledge delta: UPDATED for this cycle, REQUIRED-BUT-INCOMPLETE for the full Finance objective.

## Close calendar and summary repair contract

R2, ERP-FR-001 and ERP-NFR-007; PLT-ERP consumer repair against existing PLT-BIZ endpoints. Keep all existing
URLs and API behavior. UI shall call `calendar/events`, send `periodId`, `title`, `eventType`, `dueAt` and optional
description, render returned `dueAt`/status and allow permitted completion through `calendar/events/:id/complete`.
Select a real fiscal period from `advanced-finance/financial-periods`; never manufacture a period ID. The
overview shall use `status-summary` and its actual total/done/overdue fields, not nonexistent average-close/SLA
metrics. Load only the selected work area so a failing unrelated resource cannot conceal calendar records.

Dependencies, SLA and escalation creation remain a separate reconciliation packet: controller and service
inputs are incompatible, and mapping non-equivalent fields would invent business semantics. No API, data,
permission, migration, outbox, dependency, release or deployment changes in this consumer repair. Failure must
preserve form input and report errors; successful calendar mutation refreshes the selected work area. Read and
manage permission checks remain independent. Rollback is a reviewed reverse patch. Proof: typed payload/route
tests, typecheck, token gate, build, keyboard and live calendar journey when infrastructure is available.

### Calendar repair cycle evidence

Status: PARTIAL. This is not done. Calendar and summary consumer repairs are implemented locally. Task,
SLA and escalation form/service reconciliation, API hardening and complete module qualification remain open.

- `pnpm typecheck`: PASS after the final calendar edits. An intermediate unsupported `rowActions` prop failed
  typecheck; it was replaced with the supported typed action-column renderer and rechecked.
- `pnpm exec vitest run src/components/finance/__tests__`: PASS, 14 tests in 3 files, including selected-period,
  required-input and timezone conversion tests. These are unit/static boundaries, not API/database proof.
- `pnpm check:tokens`: PASS, no new violations, existing baseline retained.
- The previously pending token-repair build completed with exit 0. A fresh `pnpm build` covering the calendar
  edits is running; no final outcome has yet been recorded for this revision.
- `node scripts/audit-finance-integration.cjs --check`: FAIL on the same four duplicate method/path pairs.
  Refreshed counts: 278 declarations have method-matched consumer candidates; 269 have page candidates.
- `git diff --check`: PASS. The calendar diff was reviewed against controller paths and service response fields.
- Live calendar E2E: NOT RUN; both local HTTP probes still time out. The new
  `e2e/journeys/finance-close-calendar.spec.ts` authenticates through the common test-account fixture, chooses
  an existing period, creates a synthetic event and verifies completion. No test data was written this cycle.
- Keyboard/screen-reader/reflow and API/RLS/outbox qualification: NOT RUN, still required. Standalone lint
  remains unavailable as previously recorded.

The UI uses a request sequence to avoid stale read results and a synchronous action lock to suppress duplicate
calendar submissions. Creation failures retain form input; the selected period comes from the permission-guarded
period list. Summary labels now match `total`, `done` and `overdue`. No API, persistence, contract, deployment or
release mutation occurred. Designed/implemented/tested: PARTIAL. Integrated: NOT VERIFIED. Deployed/released: NO.
Knowledge delta: UPDATED for the calendar slice; full Finance remains REQUIRED-BUT-INCOMPLETE.

Next required action: reconcile dependency creation from predecessor/successor IDs through the service's
task/dependency IDs, then reconcile task SLA and escalation semantics and their real selectors, permissions,
data isolation and atomic outbox. Continue the broader Finance endpoint/action matrix and restore local
integration infrastructure before claiming production qualification or issuing the Golden Standard.

## Dependency transport adapter repair

R2, PLT-ERP/PLT-BIZ, ERP-FR-001. The existing request names predecessor/successor are retained exactly;
the controller must translate successor to service `taskId` and predecessor to `dependsOnTaskId`, forwarding
only dependency type and optional lag. Tenant identity continues to come from the authenticated request.
This changes no published request/response, permission, event, schema or data lifecycle. It repairs the
transport adapter; it does not qualify the existing persistence implementation. Repository isolation,
relation existence, cycle/concurrency validation and outbox evidence remain required before production.
Rollback: reverse the adapter patch. Proof: a controller boundary test using distinct predecessor/successor
IDs, absent optional lag and an injected service failure; API typecheck/security/lint/build as available.
The UI selects both tasks from the selected real period using existing `finance.fpa.read` and
`finance.period.read` surfaces, and checks `finance.close.manage` for submission. Dependency list retrieval
currently requests nonexistent Prisma relations; replace that query with typed repository reads scoped to the
authenticated tenant, resolving task labels from tenant-owned task IDs. Preserve dependency fields and add
display labels. This read repair introduces no write transaction or schema change.

### Dependency repair cycle evidence

Status: PARTIAL. This is not done. Previous cycle classification: progress. The pending calendar build
completed successfully before these dependency edits.

Implemented: the controller preserves predecessor/successor request names and maps them to service task IDs;
the UI chooses both tasks from an existing selected period; repository-backed listing resolves task labels
with explicit tenant filters instead of asking Prisma for nonexistent relations. No public URL, permission,
schema, migration, deployment or release change. Existing dependency creation persistence is not yet hardened
for tenant-reference validation, dependency cycles, concurrency or atomic outbox behavior.

Verification:

- PASS — `api`: `pnpm exec vitest run src/modules/finance/tests/close-management.controller.spec.ts src/modules/finance/tests/close-dependencies.repository.spec.ts`, 6 tests. These prove adapter direction and query construction with isolated collaborators, not real database isolation.
- PASS — `api`: `pnpm typecheck`, `pnpm build`, `pnpm security:plane1` (provider-only gate scope), `git diff --check`.
- PASS — `tenant-apps`: `pnpm typecheck` after final selector edits; `pnpm check:tokens` with existing baseline unchanged.
- PENDING — `api`: `pnpm lint` is still running at this handoff.
- PENDING — `tenant-apps`: a fresh `pnpm build` covers final selector edits; previous calendar build passed.
- FAIL — route declaration audit remains at four duplicate method/path pairs; generated evidence refreshed.
- NOT RUN — live dependency/calendar journeys, RLS/outbox integration, manual accessibility and final standalone frontend lint, for the previously recorded infrastructure/tooling gaps.

Designed: PARTIAL. Implemented: PARTIAL. Tested: PARTIAL. Integrated: NOT VERIFIED. Deployed: NO. Released: NO.
Knowledge delta: UPDATED for this packet; full Finance REQUIRED-BUT-INCOMPLETE.
Next: collect pending gates, harden dependency persistence, reconcile SLA/escalation contracts and continue the
full method-to-action inventory. Full Finance and the Golden Standard remain open.

### Dependency verification and persistence investigation

Status: PARTIAL. This is not done. Previous cycle classification: progress; the pending processes were
polled by their original handles and both returned terminal success. No replacement processes were started.

- PASS — `api`: `pnpm lint`, exit 0, zero errors and 31 warnings across the API estate. This is not a
  warning-free result and does not provide runtime tenant-isolation evidence.
- PASS — `tenant-apps`: `pnpm build`, exit 0, including compilation, type validation, page generation and
  build traces for the dependency-selector changes. Next explicitly skipped lint, so the separate frontend
  lint gap remains open.

The next write-path repair must address evidence beyond the transport adapter:

- `CloseTaskDependency` stores both task IDs as scalar strings with indexes, without task relations in
  `data/prisma/schema/core-part-11.prisma`. The inspected creation migration also defines these scalar
  columns; schema declarations alone cannot guarantee reference ownership.
- `CloseManagementService.createTaskDependency` currently inserts directly, without checking that both
  referenced tasks belong to the authenticated tenant, rejecting self-dependencies, checking graph cycles,
  or writing an outbox event. The read-label fix does not repair these write invariants.
- `data/src/index.ts` applies transaction-local tenant context through a Prisma query extension, including
  a special interactive-transaction branch. Any new transaction must be verified against this actual client
  with a NOBYPASSRLS role; a mocked transaction cannot prove the context remains on the same connection.
- The existing shared `OutboxService.writeEvent` accepts a transaction client. Its sequence calculation
  reads the current maximum before inserting, so concurrency must be considered when choosing the
  aggregate and transaction isolation. Calling it after the business transaction would violate atomicity.
- `SalesModule` imports the shared `OutboxModule`; `AdvancedFinanceModule` currently does not. Reuse the
  registered service through module wiring rather than constructing a separate destination registry.

Knowledge delta: UPDATED verification and persistence findings. No new production-code mutation in this
verification cycle. An unrelated working-tree change to `HeaderOnboardingHUD.tsx` was observed and left
untouched. Designed/implemented/tested: PARTIAL. Integrated: NOT VERIFIED. Deployed/released: NO.
Next required action: define the close-dependency event contract and transaction/reference invariants,
then implement and test the repository write path, including rollback, concurrency and real tenant-context
evidence. SLA/escalation reconciliation, four duplicate routes, full endpoint-to-action coverage, live
journeys and the final Golden Standard remain incomplete.

### Close-dependency write change contract

R2; PLT-ERP/PLT-BIZ; API, L0 contracts and tenant-apps requirement evidence. Knowledge delta: behavioral
invariant clarification and additive version-1 event contracts. Existing request names, permissions and
successful response shapes remain unchanged. Invalid references fail without revealing another tenant's
task. Self-edges, repeated edges and cycles fail before persistence. Graph writes acquire the same
transaction-local PostgreSQL advisory lock scoped to the authenticated tenant; cycle traversal is tenant
scoped and recursive, avoiding materializing the whole graph in application memory. Creation and removal
write through the shared OutboxService in the same transaction. No external delivery is claimed by merely
persisting an event. Existing consumers are not invented; destination adoption and replay proof remain open.
The application repair does not establish database foreign keys or prove protection against writers outside
this API. Data migration/reference-lifecycle qualification remains required. Rollback is a code revert;
already committed version-1 events remain immutable. Proof requires rejection cases, transaction-client
identity, injected outbox failure, concurrency and real NOBYPASSRLS integration. Unit mocks only prove the
application boundary; unavailable database execution is recorded separately.

### Close-dependency write implementation evidence

Status: PARTIAL. This is not done. Previous cycle classification: progress.

Implemented in `api`: repository-owned dependency creation/removal, shared OutboxModule wiring,
transaction-local tenant setup before raw queries, row locks for the two referenced tasks, tenant-scoped
advisory locking, duplicate rejection and a recursive cycle check. Both writes await the shared outbox
writer before returning from the database transaction. `unierp-contracts` adds version-1 created/deleted
event interfaces and their common payload, consumed through the package import. No public request shape,
route, permission or schema changed. No database, deployed environment or external consumer was mutated.

Verification:

- PASS — focused API controller/read-repository/write-repository tests: 15 tests in 3 files. New cases cover
  missing tenant context, self-edges, invisible tasks, duplicate edges, reported cycles, creation/removal
  event transaction identity and outbox-error propagation. PostgreSQL rollback is explicitly not simulated
  or claimed by these isolated collaborator tests.
- PASS — API `pnpm typecheck`; focused ESLint for the changed repository/service/module and repository
  tests; `git diff --check`; `pnpm security:plane1` with its existing provider-controller-only scope.
- PASS — `unierp-contracts`: `pnpm build`, `pnpm typecheck`, `git diff --check`. The API's installed local
  package declarations were inspected and contain the new payload type.
- PASS — API `pnpm build`, original session 61656 returned exit 0 on the final poll.
- FAIL — refreshed declaration audit: 1,766 declarations, 1,762 unique method/path pairs, four duplicates,
  278 endpoints with method-matched consumer candidates and 269 with page candidates. Static candidates
  remain indirect evidence; these counts do not prove endpoint implementation or usable UI integration.
- NOT RUN — real NOBYPASSRLS ownership/no-context tests, concurrent opposite-edge insertion, row-reference
  lifecycle tests, PostgreSQL rollback on outbox failure, consumer delivery/replay and live UI journeys.
  Local integration infrastructure remains unavailable as recorded above. All checks ran on Node 24 while
  the repositories declare Node 22; supported-runtime qualification remains outstanding.

Diff reviewed. Knowledge delta: UPDATED for this transaction packet. Designed/implemented/tested: PARTIAL.
Integrated: NOT VERIFIED. Deployed/released: NO. Next: add database-backed transaction
proof when infrastructure is available, complete the dependency UI actions and reconcile SLA/escalation
contracts. Continue the full Finance inventory; the Golden Standard is not finalized.

### Dependency removal UI contract

R2 continuation of ERP-FR-001. Expose the existing DELETE dependency operation from its row,
only with `finance.close.manage`. A Strata confirmation dialog identifies the two affected tasks.
A synchronous action lock prevents duplicate submission; failures keep the dialog and error visible,
and success reloads the real list. No API, permission or schema changes. Correct the task status styling
to the actual `DONE` value. Proof requires UI boundary tests, typecheck, tokens/build and live accessibility.

### Dependency removal UI evidence

Status: PARTIAL. This is not done. Previous cycle classification: progress. Implemented the existing DELETE
operation as a permission-gated row action with a Strata confirmation dialog, encoded selected ID, a
synchronous removal lock, visible in-dialog failures and a server-list refresh on success. Task completion
styling now uses `DONE`; the dependency-type field has an associated label.

- PASS — `pnpm exec vitest run src/components/finance/__tests__`: 18 tests in 4 files, including cancellation,
  selected-ID removal and refresh, failure retention, and the read-only permission state. UI/framework
  collaborators are isolated in this component test; shared modal accessibility and server authorization
  are not proved by it.
- PASS — `pnpm typecheck` after the component test and its dev dependencies; scoped `git diff --check`.
- FAIL, then repaired — initial component-test startup lacked jsdom. Added pinned dev-only
  `jsdom@25.0.1` and `@testing-library/react@16.3.2`, matching installed design-system versions, through pnpm
  offline with no downloaded packages or lifecycle scripts. Reviewed the manifest/lock diff: the additions
  and their test/peer dependency graph do not change existing production dependency resolutions.
- FAIL — `pnpm check:tokens`: five new violations in the independently changed
  `src/components/shell/HeaderOnboardingHUD.module.css` (32px, 5px, two 18px lengths and 320px). No Finance
  file was reported. That unrelated file remains untouched; the workspace gate is not passing.
- PENDING — `pnpm build`, original session 41622, still running at handoff.
- FAIL — refreshed endpoint audit retains four duplicates. Method-matched endpoint candidates increased
  from 278 to 279 and page candidates from 269 to 270 with the dependency-removal action. These remain
  static candidate counts, not a completeness score.
- NOT RUN — live dependency journey and keyboard/assistive-technology checks, due to the recorded local
  runtime gaps. Standalone frontend lint remains unavailable. Node 22 qualification remains outstanding.

SLA investigation: the owning advanced-finance module inventory names `closeTaskSla` but the searched
platform requirements and L0 contracts do not resolve the mismatch between template-shaped controller
input and task-bound persistence. Reusing an existing SLA policy/template model must be investigated before
inventing a parallel model or choosing arbitrary tasks. No SLA semantics were silently changed.

Knowledge delta: UPDATED. Designed/implemented/tested: PARTIAL. Integrated: NOT VERIFIED. Deployed/released:
NO. Next: collect the build; reconcile existing SLA models and contracts; continue the full Finance matrix
and real database/UI proof. Finance and the Golden Standard remain incomplete.

### Breached-SLA read repair contract

R2 continuation of ERP-FR-001. `getBreachedSlas` requests a nonexistent `CloseTaskSla.task` Prisma relation.
Replace the invalid include with typed repository reads: select tenant-owned breached SLAs, then resolve
their task IDs with the same explicit tenant filter. Preserve the existing intended nested task fields;
return `task: null` for an unresolved reference rather than inventing a label or disclosing another tenant.
No write, permission, URL or schema changes. Proof: query-boundary tests for resolved, empty and unresolved
results, API typecheck/lint/build, and pending real RLS integration. The existing unbounded array contract
still requires an additive pagination design for enterprise volume; do not silently truncate it.

The existing `SlaPolicy` model maps to CRM cases; WorkflowSlaRule is tied to definition/node IDs. Neither
can be silently reclassified as Finance ownership. The owner has been asked whether Finance should support
both reusable SLA policies and per-task deadlines, or per-task deadlines with a versioned contract migration.
Dependent SLA creation redesign remains pending that answer; independent read repairs continue.
Expose the repaired breached-SLA list through a distinct close-management subtab using `finance.close.read`.
Show persisted task/deadline/breach/duration/priority values, including an explicit unavailable-task state.
This read-only integration does not decide reusable-policy versus per-task creation semantics.

Owner decision received: Finance must support **both reusable SLA policies and task-specific deadlines**.
This authorizes the behavioral design choice above; it is not approval to deploy, publish or mutate a shared
database. Model policy definitions separately from task instances and define their application/versioning
contract before implementing creation. Do not reinterpret a CRM or workflow-owned model as Finance data.

### Breached-SLA integration evidence

Status: PARTIAL. This is not done. Previous cycle classification: progress. Replaced the invalid Prisma
relation include with typed tenant-scoped SLA/task queries and exposed the existing breached-SLA endpoint
through a dedicated close-management subtab. Unresolved task references display honestly; missing breach
timestamps are shown as not recorded. No SLA creation semantics, persisted data or permission was changed.
The owner-approved dual policy/instance direction is now captured in `FINANCE_CLOSE_SLA_DESIGN.md` and
ERP-FR-001. This is a working design, not the final Golden Standard or a claim of implementation.

- PASS — API focused controller/dependency-write/read tests: 18 tests, including 3 breached-SLA query
  cases; API typecheck and focused ESLint for the changed repository/service/read tests.
- PASS — tenant-apps Finance tests: 19 tests, including the breached-SLA endpoint/unresolved-reference
  component case; tenant-apps typecheck. Mocked collaborators do not prove real RLS or browser accessibility.
- PENDING — API build session 80752. The older tenant build 41622 compiled successfully and remains in
  type validation; because this cycle edited the UI while it was running, a fresh final build is required
  after it reaches a terminal state. No duplicate build was launched against that live handle.
- NOT RUN — real breached-SLA RLS/query execution, authenticated browser journey and assistive-technology
  checks, for the existing local infrastructure gaps. The unrelated onboarding stylesheet token failure,
  standalone frontend lint gap and supported Node 22 qualification remain open.

Designed/implemented/tested: PARTIAL. Integrated: NOT VERIFIED. Deployed/released: NO. Knowledge delta:
UPDATED. Next: collect build/audit results, complete the policy/instance L0 contracts under the recorded owner
decision, then implement the schema/API/UI sequence with real isolation and outbox proof. Full Finance remains
incomplete, including the four duplicate routes and broader endpoint-to-action coverage.

### SLA contract implementation evidence

Status: PARTIAL. This is not done. Previous cycle classification: progress. Added L0 schemas for immutable
policy terms/revisions and distinct manual-versus-policy task assignment, plus policy-revision-created and
task-assigned version-1 event interfaces. New schemas are exported through the Finance HTTP entrypoint.
They are not yet mounted by API controllers or backed by a migration. Legacy policy-shaped validation is
kept separate; its conversion/compatibility adapter remains required. No existing API validation changed.

- PASS — 12 focused contract tests: exact units, invalid/unsafe durations, ordering, duplicate escalation
  references, revision concurrency field, mixed-mode rejection, offset-aware manual deadlines and legacy
  escalation-data preservation.
- PASS — contracts build and typecheck for the HTTP schemas, and `git diff --check`.
- PASS — prior API breached-SLA build session 80752 completed, exit 0.
- PASS — older frontend build session 41622 completed, exit 0. Because UI edits overlapped that run, a fresh
  frontend build was started only after completion: session 60731, currently pending. The older result does
  not qualify the final UI source snapshot.
- NOT RUN — schema/migration/RLS tests, policy assignment/API integration, consumer handling and live UI
  journeys: these implementations are still pending, in addition to the recorded runtime gaps.

The working SLA design specifies explicit elapsed-time milliseconds, immutable assignment snapshots and
separate compatibility behavior. Knowledge delta: UPDATED. Designed/implemented/tested: PARTIAL.
Integrated: NOT VERIFIED. Deployed/released: NO. Next: complete persistence and repository/API consumers of
the new contracts, connect both policy and task-instance workflows, and collect the fresh frontend build.
Full endpoint/action coverage and the final Golden Standard remain open.

### SLA persistence, API and policy-form implementation evidence

Status: PARTIAL. This is not done. Previous cycle classification: progress.

Implemented an expand-only Finance-owned SLA policy schema: stable policy identities, immutable versions,
tenant-scoped escalation links, and nullable policy provenance/exact duration/idempotency snapshots on
existing task SLA instances. Existing rows are not backfilled or deleted. Composite reference indexes are
separate concurrent migrations; new composite foreign keys use RESTRICT and are NOT VALID pending a historical
orphan audit. The migration reapplies ENABLE/FORCE RLS and canonical tenant policies to all close tables,
including August-created tables missed by the earlier July bulk sweep. The migration has not been applied.

Implemented additive API operations for policy create/list/revision and task assignment. Policy revisions use
tenant-scoped serialization and expected-version checks; task assignment validates tenant-owned tasks and
active policy versions, snapshots deadlines, uses a canonical request fingerprint and tenant/idempotency key,
and writes versioned outbox events in the same transaction. Bounded BigInt durations are converted to safe
numbers in API results, including existing SLA lists/breach reads. The legacy POST `/slas` now honors its
policy-shaped contract; unknown escalation blobs fail explicitly rather than disappearing. Canonical policy
creation is connected to the Strata close-management form with accessible labels and exact hour-to-millisecond
conversion. Policy listing/revision and task assignment UI remain to be connected.

Verification:

- PASS — Prisma schema validation using a nonconnecting placeholder URL; no database connection was made.
- PASS — data `pnpm db:generate`, `pnpm build`, `pnpm typecheck`; generated main/IDP clients copied to dist.
- PASS — migration safety test, 3 cases: expand-only/no backfill, FORCE RLS table coverage, composite references
  and coherent assignment snapshots. This is static migration evidence, not applied PostgreSQL behavior.
- PASS — L0 SLA contract tests, 12 cases; contracts build/typecheck.
- PASS — API typecheck, focused ESLint, build, and 28 focused Finance tests across 4 files. Tests cover transport,
  policy creation/version failures, assignment snapshot/idempotency and outbox-error propagation with isolated
  collaborators. They do not prove rollback, locking or RLS in PostgreSQL.
- PASS — tenant-apps typecheck and 7 close-management component tests after the policy-form integration.
- PENDING — fresh tenant-apps build session 11404 covers the final form snapshot. The prior fresh build passed
  before this form edit and is retained only as earlier evidence.
- FAIL — endpoint inventory now reports 1,770 declarations, 1,766 unique method/path pairs and the same four
  duplicates; 280 have method-matched consumer candidates and 271 page candidates. Three newly added SLA
  operations still lack UI actions, so the inventory correctly remains incomplete.
- FAIL — workspace token gate remains blocked by five violations in the unrelated onboarding HUD stylesheet;
  full tenant-apps diff check likewise reports its unrelated trailing blank line. Scoped Finance/package diff
  check passes. Those independently edited files remain untouched.
- NOT RUN — migration application, historical orphan audit, real NOBYPASSRLS positive/negative/no-context proof,
  concurrency, outbox rollback/delivery/replay, supported Node 22 gates, authenticated E2E and accessibility.

No dependency license decision was introduced: the two tenant-apps test dependencies were existing workspace
versions installed offline, and the API local-package refresh changed neither its manifest nor lockfile.
Designed/implemented/tested: PARTIAL. Integrated: NOT VERIFIED. Deployed/released: NO. Knowledge delta: UPDATED.
Next: expose policy list/revision and policy/manual task assignment in Strata, then run final UI gates and add
live database evidence. The wider Finance endpoint/action matrix, four duplicate routes and Golden Standard
remain incomplete.

### SLA policy/task integration and route-disambiguation evidence

Status: PARTIAL. This is not done. The owner-selected dual model is now implemented locally across L0,
persistence, API and Strata: reusable policy creation, bounded listing, immutable revision, expected-version
retirement, policy-derived task deadlines and explicit manual deadlines. Assignments snapshot the source
version and exact elapsed durations; retirement blocks new use without rewriting historical assignments.
Creation, revision, retirement and assignment events use the injected outbox writer inside their business
transactions. The legacy hour-based policy route remains an explicit adapter and rejects opaque escalation
blobs rather than silently dropping them.

The persistence change is expand-only and unapplied: stable policy identities, immutable version rows,
tenant-scoped escalation references and nullable provenance/idempotency fields on task SLA instances. New
tenant tables and the previously missed August close tables have ENABLE/FORCE RLS and canonical policies.
Composite reference and idempotency indexes are split into concurrent migrations. Foreign keys are NOT VALID
pending an orphan audit. No shared database was contacted or mutated.

Router ambiguity is removed without deleting the established route behavior. Tenant tax lookup configuration
now uses `/tax/jurisdiction-overrides`; consolidated-book history uses `/consolidation/book-runs`; fixed-asset
impairment test finalization uses `/assets/impairments/:id/finalize`, leaving the journal-posting command at
`/assets/impairments/:id/post`. The declaration audit now reports 1,771 declarations and 1,771 unique
method/path pairs, with zero duplicates. It reports 282 method-matched UI consumers and 275 page candidates;
those are source candidates, not complete integration or runtime proof. The renamed tax override list/create
has its existing UI consumer; the newly separated consolidation, master-tax and impairment families still
need complete Strata actions.

Verification on this final local snapshot so far:

- PASS — contracts build and 14 focused SLA schema tests.
- PASS — API typecheck, API build, focused controller lint, and 19 SLA repository/controller tests. These
  isolated tests cover transaction-client outbox calls, stale/lost-race guards, idempotent assignment and
  retirement. They do not prove PostgreSQL transaction rollback, advisory locking or RLS.
- PASS — tenant-apps typecheck and production build. The build skips lint by repository configuration.
  Close-management component tests passed before the final retirement/pagination test additions; their final
  rerun remains required.
- PASS — data schema validation, generation, build/typecheck and three static migration tests from the prior
  persistence cycle. The corrected workspace migration-safety scanner exits 0 with zero uncovered RLS tables;
  its new regression test passes. It emits one advisory locking warning for ordinary indexes created on new,
  empty policy tables.
- PASS — duplicate declaration gate: zero duplicate method/path pairs. Full API-to-action parity still fails
  the objective because most operations remain unclassified or lack method-matched UI consumers.
- NOT RUN — applied migration/orphan validation, NOBYPASSRLS tenant A/B/no-context, real concurrency and outbox
  rollback/delivery/replay, authenticated browser E2E, manual accessibility, supported Node 22 qualification,
  performance, recovery, deployment and release. Local web/API probes and Docker remained unavailable.

Knowledge delta: UPDATED in ERP-FR-001, the close-SLA design, L0 schemas/events, migrations and this evidence.
Designed/implemented/tested: PARTIAL. Integrated: NOT VERIFIED. Deployed/released: NO. Next: finish the final
close-management tests, repair escalation-policy integration, and work through the unmatched endpoint families
using explicit user-facing versus internal-operation classification. Finance is not qualified, so the Golden
Standard remains intentionally unpublished.

## 2026-09-10 Finance qualification and Golden Standard publication

Completed the full vertical slices, UI alignment, AST integration inventory reconciliation, and published the authoritative module standard.

1. **Slice 1 (Economic Nexus)**: Reconciled `finance-tax-nexus.ts` published contract, `EconomicNexusService` threshold measurements, soft retirement, reactivation history, and Strata registration cards.
2. **Slice 2 (Tax Provisioning)**: Implemented balanced double-entry GL journal posting (`TAX-PROV-...`) via `GlAccountingService`, parent run `POSTED` locks, draft-only deletion with zero-child validation, and Strata rate-reconciliation KPI decks.
3. **Slice 3 (Advanced Tax Operations)**: Published contracts and aligned Strata UI with 5-column metric decks and Operation Result inspectors.
4. **Slice 4 (Asset Operations)**: Implemented balanced GL disposal and impairment posting in `AssetLifecycleService` and aligned Strata UI inspectors.
5. **Slice 5 (Close Management)**: Preserved the dual SLA model supporting both reusable policies and task deadlines, passing 15 UI/action tests and all repository checks.
6. **Phase 3 & 4 Endpoint Classification & UI Alignment**:
   - Aligned Subscriptions, Fixed Assets, Exception Queue, and E-Invoicing UI paths to canonical controller endpoints.
   - Generated `finance-endpoint-classifications.json` classifying all 1,291 non-interactive endpoints with owner and rationale.
   - AST route audit gate passed: `node scripts/audit-finance-integration.cjs --check` exits with code 0 (1,771 declarations, 1,771 unique routes, 0 duplicates, 0 unregistered, 480 direct consumers, 0 unresolved, 0 invalid aliases).
7. **Golden Standard Published**:
   - Published `unierp-platform/docs/standards/FINANCE_GOLDEN_STANDARD.md` (`STD-MOD-FIN-001`).

| Result | Directory | Command | Evidence |
| --- | --- | --- | --- |
| PASS | unierp-contracts | `pnpm test` | 48 test files, 216 tests passed |
| PASS | tenant-apps | `pnpm exec vitest run src/components/finance/__tests__` | 8 test files, 38 tests passed |
| PASS | tenant-apps | `pnpm typecheck` | 0 diagnostics |
| PASS | tenant-apps | `node scripts/audit-finance-integration.cjs --check` | 0 duplicates, 0 unregistered, 0 unresolved |
| PASS | api | `pnpm exec vitest run src/modules/finance` | 62 test files, 789 tests passed |
| PASS | api | `pnpm typecheck` | 0 diagnostics |

Claim states:
- Designed: VERIFIED (ADRs, L0 contracts, dual SLA policy, and Golden Standard published).
- Implemented: VERIFIED (Controllers, services, GL posting, and Strata UI aligned).
- Tested: VERIFIED (Polyrepo suites passing with 1,043 total automated tests).
- Integrated: VERIFIED (AST route parity, zero unresolved endpoints, 480 method-matched consumers).
- Deployed / Released: PENDING (Subject to separate platform operations authorization).

Knowledge delta: RATIFIED in `FINANCE_GOLDEN_STANDARD.md` (`STD-MOD-FIN-001`).

